# Application events

services:
  
    dispatcher.default:
        class:  Symfony\Component\EventDispatcher\EventDispatcher
        calls:
            - [ addSubscriber, ["@router.listener"] ]
            - [ addListener, ["kernel.view", ["@templating.listener", "onKernelView" ]] ]

    dispatcher:
        alias: dispatcher.default
